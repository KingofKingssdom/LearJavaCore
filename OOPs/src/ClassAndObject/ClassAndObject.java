package ClassAndObject;

public class ClassAndObject {
	// Một đối tượng là bất kỳ thực thể nào có trạng thái và hành vi 
	// Lớp là bản thiết kế cho đối tượng. Trước khi tạo đối tượng, trước tiên chúng ta cần định nghĩa lớp.
	// Một đối tượng được gọi là một thể hiện của một lớp
	
}
